language: php

php:
  - 5.6
  - hhvm # on Trusty only
  - nightly

install:
  - composer install

test:
    stage: test
    script:
        - php vendor/bin/phpcs -n -p --extensions=php --standard=PSR2 ./application
pages:
  stage: deploy
  dependencies:
    - test
  only:
    - master  

       